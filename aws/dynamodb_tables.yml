Resources:
  ItemsDynamodbTable:
    Type: AWS::DynamoDB::Table
    UpdateReplacePolicy: Retain
    Properties:
      TableName: ${self:provider.stage}-${self:service.name}-items
      BillingMode: PAY_PER_REQUEST
      KeySchema:
        - AttributeName: itemID
          KeyType: HASH
      AttributeDefinitions:
        - AttributeName: itemID
          AttributeType: S
