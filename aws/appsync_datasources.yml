- type: AWS_LAMBDA
  name: getHelloQuery
  config:
    functionName: getHelloQueryFn
    serviceRoleArn: !GetAtt AppSyncServiceRole.Arn

- type: AWS_LAMBDA
  name: addHelloMutation
  config:
    functionName: addHelloMutationFn
    serviceRoleArn: !GetAtt AppSyncServiceRole.Arn
